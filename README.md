Doku erzeugen:
```pdoc outlier_detection outlier_detection/simple -d numpy -o doc```

Requirements aktualisieren:
pip freeze > requirements.txt

Virtuelle Umgebung erzeugen:
python -m venv .venv

Bibliotheken installiere:
pip install -r requirements.txt